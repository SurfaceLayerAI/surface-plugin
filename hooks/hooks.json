{
  "description": "Surface plugin hooks for session indexing",
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "SURFACE_INDEXING=1 python3 ${CLAUDE_PLUGIN_ROOT}/scripts/index_session.py"
          }
        ]
      }
    ]
  }
}
